# Copy to .env and fill in. .env is gitignored - never commit the real key.
CR_API_TOKEN=paste_your_jwt_here

# Use the RoyaleAPI proxy if your IP is dynamic (residential internet).
# proxy  -> https://proxy.royaleapi.dev/v1   (allowlist 45.79.218.79 on the key)
# direct -> https://api.clashroyale.com/v1   (allowlist your own public IP)
CR_API_BASE=https://proxy.royaleapi.dev/v1
